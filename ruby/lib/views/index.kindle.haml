%a{:name => 'TOC'}
- books_grouped.each do |testament, groups|
  %center
    %h2= testament
  - groups.each do |name, books|
    %center
      %h3= name
      %p{:align => 'center'}
        = books.map {|book| "<a href='#book_#{book.number}' class='book_name_link'>#{book.name}</a>" }.join(', ')
  :erb
    <mbp:pagebreak />